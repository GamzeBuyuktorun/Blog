@model BlogProject.Models.Blog

@{
    ViewData["Title"] = "Yeni Blog Oluştur";
}

<div class="row justify-content-center">
    <div class="col-md-8">
        <div class="card">
            <div class="card-header">
                <h4><i class="fas fa-plus-circle"></i> Yeni Blog Oluştur</h4>
            </div>
            <div class="card-body">
                @if (!ViewData.ModelState.IsValid)
                {
                    <div class="alert alert-danger">
                        <h6>Hatalar:</h6>
                        <ul class="mb-0">
                            @foreach (var error in ViewData.ModelState.Values.SelectMany(v => v.Errors))
                            {
                                <li>@error.ErrorMessage</li>
                            }
                        </ul>
                    </div>
                }

                <form asp-action="Create" method="post">
                    @Html.AntiForgeryToken()
                    
                    <div class="mb-3">
                        <label asp-for="Title" class="form-label">Blog Başlığı *</label>
                        <input asp-for="Title" class="form-control" placeholder="Blogunuzun başlığını girin" required />
                        <span asp-validation-for="Title" class="text-danger"></span>
                    </div>
                    
                    <div class="mb-3">
                        <label asp-for="Description" class="form-label">Blog Açıklaması</label>
                        <textarea asp-for="Description" class="form-control" rows="3" 
                                  placeholder="Blog hakkında kısa bir açıklama (isteğe bağlı)"></textarea>
                        <span asp-validation-for="Description" class="text-danger"></span>
                        <div class="form-text">Bu açıklama blog listesinde görünecektir.</div>
                    </div>
                    
                    <div class="d-flex gap-2">
                        <button type="submit" class="btn btn-primary">
                            <i class="fas fa-save"></i> Blog Oluştur
                        </button>
                        
                        <a href="@Url.Action("MyBlogs", "Blog")" class="btn btn-outline-secondary">
                            <i class="fas fa-times"></i> İptal
                        </a>
                    </div>
                </form>
            </div>
        </div>
        
        <div class="mt-4">
            <div class="alert alert-info">
                <h6><i class="fas fa-info-circle"></i> Bilgi</h6>
                <ul class="mb-0 small">
                    <li>Blog oluşturduktan sonra blog yazıları ekleyebilirsiniz</li>
                    <li>Blog başlığından otomatik olarak URL oluşturulur</li>
                    <li>Blogları istediğiniz zaman düzenleyebilir veya silebilirsiniz</li>
                </ul>
            </div>
        </div>
    </div>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}